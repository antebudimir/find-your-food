import styled from 'styled-components';

export const RecipeDetailContainer = styled.p`
	margin-top: ${(props) => props.theme.spacing.primary};
`;

export const RecipeDetailText = styled.span`
	font-family: 'Raleway Bold';
`;
