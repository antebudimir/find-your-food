import styled from 'styled-components';

export const RecipeListContainer = styled.ul`
	margin-top: ${(props) => props.theme.spacing.primary};
`;

export const RecipeListText = styled.span`
	font-family: 'Raleway Bold';
`;
