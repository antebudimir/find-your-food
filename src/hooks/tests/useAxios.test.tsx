import { act, renderHook } from '@testing-library/react-hooks';
// import useAxios from '../useAxios';

// export default {
// 	get: jest.fn(() => Promise.resolve({ data: [] })),
// };

describe('useAxios custom hook', () => {
	test.todo('fetches recipes by correct url passed via endpoint prop');
});

describe('While fetching data', () => {
	test.todo('if it handles loading state correctly');
});

describe('When data fetched successfully', () => {
	test.todo('if it handles successful state correctly');
});

describe('An error happens during request', () => {
	test.todo('if it handles error state correctly');
});
